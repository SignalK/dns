# DNS configuration for signalk.org domain

Validate configuration

```
./run.sh check
```

Preview changes

```
TOKEN=xyz USER=xyz ./run.sh preview
```

Push changes

```
TOKEN=xyz USER=xyz ./run.sh push
```
